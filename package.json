{
  "name": "epos-todolist",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd src; browser-sync start --no-notify --no-ghost-mode --server --files .",
    "start-no-autorefresh": "cd src; browser-sync start --no-notify --no-ghost-mode --server --files . --reload-delay 99999999"
  },
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.27.10"
  }
}

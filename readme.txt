1. import .css file from .js file via require('app.css')
2. bem class plugin
   style: [ui.box.column, ui.card, { name: 'item', done: item.done$ }])
3. add dls's background: var(--bcg-primary);

3. add Store.observe(selector, listener)
4. add store actions to be dispatched and update ui

5. add state to components
6. pass props from store to components

7. synch store and component states to local-storage in dev mode